const Settings = () => {
  return (
    <div>
      <h1 className="text-xl">Settings</h1>
      <button className="btn btn-accent">Set term dates</button>
    </div>
  );
};

export default Settings;
