version: "3.8"
services:
  postgres:
    image: postgres.latest
    environment:
      POSTGRES_USER: ${{secrets.TEST_DB_USER}}
      POSTGRES_PASSWORD: ${{secrets.TEST_DB_PASSWORD}}
      POSTGRES_DATABASE: ${{secrets.TEST_DB_NAME}}
    ports:
      - "5432:5432"
